[package]
name = "mcp"
version = "0.1.0"
edition = "2021"

[dependencies]
iw_core = { path = "../core" }
tokio = { version = "1.0", features = ["io-util", "io-std", "macros", "rt-multi-thread"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
async-trait = "0.1"
