apiVersion: v1
kind: Service
metadata:
  name: {{ include "ironwarden.fullname" . }}
  labels:
    {{- include "ironwarden.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http-gateway
  selector:
    {{- include "ironwarden.selectorLabels" . | nindent 4 }}
